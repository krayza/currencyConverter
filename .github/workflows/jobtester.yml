name: Job Tester

on:
  pull_request:
    types: [ labeled ]
    
jobs:
  job-test:
    name: Job Test
    runs-on: ubuntu-latest

    env:
      github_head_ref: ${{ github }}
      github_

    steps:
      - name: Print event
      	run: |
	  echo "${{ github.head_ref }}"
	  echo "${{ github.base_ref }}"
